/***************************************	PowerPCRequired is a 680x0 code resource to alert	a user that this application cannot run on their	680x0 Macintosh	Copyright 1995-2014 by Rebecca Ann Heineman becky@burgerbecky.com 	It is released under an MIT Open Source license. Please see LICENSE	for license details. Yes, you can use it in a	commercial title without paying anything, just give me a credit.	Please? It's not like I'm asking you for money!	MacOS 8.6 and previous 680x0 machines will display	a cryptic message of "Error 3" when a PowerPC	only application is run. By including	PowerPCRequired.rsrc to your application,	on ancient 680x0 machines, the user will	see a dialog that says "The program	requires a PowerPC or Intel processor."		It's far better to see that instead of Error 3.		Keep this program short and to the point		Notes:		I copied over the 680x0 compiler from CodeWarrior 7 	and invoke it from IDE 8.3	Please note, CodeWarrior 9 and higher will NOT work	with the CodeWarrior 7 680x0 compiler. As a result, you	must compile this app with IDE 7.0-8.3			***************************************/#include <Dialogs.h>/* Must match the ID in the 'DLOG' resource in PowerPCRequiredDialog.r */#define kDialogID 32000/***************************************	Main entry point	Please note the this code is written for	MacOS 7.5.3 Classic. There is no carbonization	done since it is intended for MacOS 68k only.	***************************************/int main(void){	DialogRef MyDialog;		/* Init MacOS */		InitGraf(&qd.thePort);	InitFonts();	InitWindows();	InitMenus();//	TEInit();	InitDialogs(0);	InitCursor();	/* Invoke the dialog. */	/* See PowerPRequiredDialog.r for the text and window dimensions */	MyDialog = GetNewDialog(kDialogID,0,kFirstWindowOfGroup);	if (MyDialog) {		short ItemHit;		/* Junk */		SetDialogDefaultItem(MyDialog,1);	/* Default for OK button */		ModalDialog(0,&ItemHit);			/* Handle the dialog (There is only one button) */		DisposeDialog(MyDialog);			/* Kill the dialog */	}	return 0;				/* Exit */}